	FsGuiObject::defRoundRadius=0.0; // Changing it to no radius, for the new style buttons.
	//FsGuiObject::defRoundRadius=(double)opt.fontHeight/3.0; // Changing it to no radius, for the new style buttons.
	FsGuiObject::defHScrollBar=opt.fontHeight;
	FsGuiObject::defHAnnotation=opt.fontHeight*9/10;

	FsGuiObject::scheme=FsGuiObject::PLAIN;

	FsGuiObject::defDialogBgCol.SetIntRGBA(255,255,128,255); //Pale yellow

	// FsGuiObject::defTabBgCol.SetDoubleRGB(0.82,0.82,0.82);
	FsGuiObject::defTabBgCol.SetIntRGBA(255,255,0,255);//Bright yellow Main box colour
	FsGuiObject::defTabClosedFgCol.SetIntRGBA(255,128,0,255); //Orange
	FsGuiObject::defTabClosedBgCol.SetIntRGBA(255,0,0,2555); //Red

	FsGuiObject::defBgCol.SetIntRGBA(125,125,125,200); //default background colour. Dark green -Lower colour on inactive buttons(and flip)
	FsGuiObject::defFgCol.SetIntRGB(0,0,0); //Default text colour
	FsGuiObject::defActiveBgCol.SetIntRGBA(125,125,125,255);//poisin green (Not upper flip.. not sure.)
	FsGuiObject::defActiveFgCol.SetIntRGB(128,128,0); //Not sure, but it's brown now
	FsGuiObject::defFrameCol.SetIntRGBA(0,128,255,120); //The frame colour. Doesn't seem to work with transparency. It's now light blue

	FsGuiObject::defListFgCol.SetIntRGB(0,0,0); //Menu items, not selected (text colour)
	FsGuiObject::defListBgCol.SetIntRGBA(128,255,255,255); /// Menu items, not selected (bg colour) Now it's light turqoise
	FsGuiObject::defListActiveFgCol.SetIntRGB(128,0,255); //Text colour on selected menu item Now it's purple - Doesn't work on the top menu items, they're always white.
	FsGuiObject::defListActiveBgCol.SetIntRGB(255,128,255); //Background colour on selected menu item, doesn't apply to main menu.